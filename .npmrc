# Allow build scripts for specific packages
# These packages require build scripts to function correctly
# Using onlyBuiltDependencies in package.json to whitelist specific packages
# This is more secure than allowing all packages

